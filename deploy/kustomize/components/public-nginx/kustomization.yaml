apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Optional public-facing nginx that exposes the app externally (LoadBalancer) and routes
# /api + /ws to the backend and everything else to the frontend. Enable it from an overlay:
#
#   components:
#     - ../../components/public-nginx
resources:
  - nginx.yaml
