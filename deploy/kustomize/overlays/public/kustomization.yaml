apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base app + the optional public nginx entrypoint.
# Apply with:  kubectl apply -k deploy/kustomize/overlays/public
resources:
  - ../../base

components:
  - ../../components/public-nginx
