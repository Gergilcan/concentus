{
  "//": "Type-checks the tests alongside the sources they exercise. Separate from tsconfig.json on purpose: that one is the build (rootDir src, emits dist/), and the tests must never end up inside the packaged app.",
  "compilerOptions": {
    "target": "ES2023",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2023", "DOM"],
    "types": ["node"],
    "strict": true,
    "noEmit": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "esModuleInterop": true,
    "skipLibCheck": true
  },
  "include": ["src/**/*.ts", "test/**/*.ts", "e2e/**/*.ts", "vitest.config.ts", "playwright.config.ts"]
}
